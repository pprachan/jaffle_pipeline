sources:
  - name: jaffle_shop
    database: JAFFLE_SHOP
    schema: RAW        
    tables:
      - name: raw_customers
        freshness: null 
        columns:
          - name: id 
            data_tests:
              - unique 
              - not_null
      - name: raw_orders
        columns:
          - name: id 
            data_tests:
              - unique 
              - not_null
        config: 
          freshness: null
      - name: raw_payments